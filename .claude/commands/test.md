Run `npm run test` from the project root.

Summarize the results concisely:
- Total test count, pass count, fail count
- For any failures: test name, file, and error message
- Keep the summary under 20 lines
